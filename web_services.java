""" In this course we'll refer to a web service as the TECHNOLOGY which allows communication
of two different devices over the internet. It is NOT A PHYSICAL DEVICE, but a STANDARD
OF COMMUNICATION."""

**WEB SERVICE CONTAINS TWO COMPONENTS
*service provider (application server);
*service requestor (Client);

"""In simple terms, Web Service is the technology which allows COMMUNICATION between client and service
provider using a common language(HTTP)."""

** WEB SERVICES ARE DIVIDED INTO TWO BROAD CATEGORIES
* SOAP-based web services (Simple Object Access Protocol);
* REST-based web services (Representational State Transfer);

"""The main difference between these two is their underlying implementation."""
** SOAP IS A PREDEFINED SET OF RULES , IT IS XML BASED AND USES AN ENVELOPE TO ENCAPSULATE DATA
** REST is not a protocol but an architectural design. This design is used to send a message in 
**client and server. It can use XML ,JSON , and text to transfer data from client to server. 

**SUMMARY
"""1. create application and exposes its API which follows either SOAP or REST. 
Deploy this application in a 2. WEB SERVER (windows/linux). Somewhere in the world there is
an HTTP client. 3. CLIENT(www.google.com) this client sends request to the web-server by using 
SOAP/ REST (the client calls the API exposed by the server.)
The web-server then returns the API response.
""



